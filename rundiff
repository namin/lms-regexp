if [ -f test-out/v8regexp0 ]
then
  diff test-out/v8regexp0 test-out/v8regexp0.check > test-out/v8regexp0.diff
else
  if [ -f test-out/v8regexp0.diff ]
  then
    rm test-out/v8regexp0.diff
  fi
fi


if [ -f test-out/v8regexp1 ]
then
  diff test-out/v8regexp1 test-out/v8regexp1.check > test-out/v8regexp1.diff
else
  if [ -f test-out/v8regexp1.diff ]
  then
    rm test-out/v8regexp1.diff
  fi
fi
